#include "slist.h"

// Deque wrapper functions
// Wrapper functions for the deque operations

int main(void) {
    // Use predefined filename
    char* filename = "names.txt";


    // TODO:
    // Open the file FILE fp*;
    // Check if the file is opened
    
    
    

    Bool done = T;
    while (!done) {
        // TODO:
        // Use fscanf to read the names from the file
        // Use printf to print the names to the console
        // Use free to free the allocated memory if fscanf failed
        // Use fclose to close the file
    }
    
    //fclose(fp); Close the file
    
    
    // Main program loop
    printf("To scroll through the names type\n");
    printf("f: forwards, b: backwards, q: quit\n");
    
    char choice;
    while (1) {
        printf("\n");

        //TODO:
        // Use scanf to read the choice from the console
        // Use getchar to consume the newline character
        // Use if statements to check the choice
        // Use printf to print the choice to the console
        // Use break to exit the loop if the choice is 'q'
        // Use else if statements to check the choice
        // Use printf to print the choice to the console
        // Use break to exit the loop if the choice is 'f' or 'b'
        // Use else to print an error message if the choice is invalid

        if(1)
        {
            printf("Use break to exit the while loop.\n");
            break;
        }
    }
        
    return 0;
}
